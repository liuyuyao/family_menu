# 帆帆菜单 — 开发计划

## 1. 项目背景

家庭场景：做饭阿姨每天做一顿晚饭（约1.5h），四菜一汤。用户每天需要：
1. 决定当天菜单（考虑家人喜好、前几天不重复、营养搭配）
2. 根据菜单网购食材
3. 把菜单和做法告诉阿姨

## 2. 技术方案

| 维度 | 选型 | 理由 |
|------|------|------|
| **形态** | PWA 网页（单页应用） | 零服务器、零运维、手机/电脑都能用、可离线、可添加到桌面 |
| **数据持久化** | localStorage | 无后端，数据存在本地；支持导出 JSON 备份 |
| **菜谱数据源** | `recipes.csv` + 程序内嵌默认数据 | 用户可用 Excel 维护 CSV，通过"导入"功能更新；App 也支持直接增删改 |
| **样式框架** | 纯 CSS（无第三方库） | 减少依赖、离线可用、体积最小 |
| **部署** | 静态文件（可放 GitHub Pages / 本地直接打开） | 双击 `index.html` 即可运行 |

## 3. 文件结构

```
fanfan_menu/
├── index.html              # 单页应用入口（所有页面片段）
├── css/
│   └── style.css           # 响应式样式，移动优先
├── js/
│   ├── app.js              # 主逻辑、页面路由、状态管理
│   ├── data.js             # CSV 解析、localStorage 封装、导入导出
│   ├── engine.js           # 推荐引擎算法
│   └── pwa.js              # Service Worker 注册与缓存策略
├── data/
│   └── recipes.csv         # 用户维护的菜谱源文件
├── icon.png                # PWA 桌面图标
├── manifest.json           # PWA 配置（名称、图标、主题色）
├── sw.js                   # Service Worker（离线缓存）
└── AGENTS.md               # 本开发计划
```

## 4. 数据模型

### 4.1 菜谱（Recipe）
来源于 `recipes.csv`，解析后存入内存：

```js
{
  id: "糖醋排骨",
  name: "糖醋排骨",
  category: "big_meat",      // big_meat | small_meat | vegetable | soup | staple
  mainIngredient: "猪肉",
  preference: 4,             // 1~5，喜爱度
  cookTime: 60,              // 分钟
  difficulty: "medium",      // easy | medium
  ingredients: [             // 从食材1~5解析
    { name: "猪肋排", amount: "500", unit: "g" },
    { name: "冰糖", amount: "30", unit: "g" }
  ],
  lastEaten: "2026-05-10"    // 上次吃的日期，程序维护
}
```

### 4.2 历史菜单（Menu）
存入 localStorage：`key = "menus"`

```js
{
  date: "2026-05-14",
  recipes: ["红烧鸡翅", "番茄炒蛋", "手撕包菜", "玉米排骨汤", "米饭"],
  source: "auto"              // auto=推荐生成 | manual=手动调整
}
```

### 4.3 库存食材（Inventory）
存入 localStorage：`key = "inventory"`

```js
[
  { name: "鸡蛋", quantity: "10", unit: "个", addedDate: "2026-05-14" },
  { name: "番茄", quantity: "3", unit: "个", addedDate: "2026-05-14" }
]
```

## 5. 功能模块

### 5.1 今日推荐（首页）
- **一键生成菜单**：调用推荐引擎，生成四菜一汤+主食
- **菜单卡片展示**：每道菜显示名称、分类标签、喜爱度、用时、上次吃的时间
- **换菜**：点击单道菜可"换一道"，从同分类候选池中替换
- **固定+重排**：可固定某几道菜，其余重新生成
- **确认今日菜单**：写入历史记录，更新各菜的 `lastEaten`
- **采购清单**：汇总菜单所需食材，减去库存，生成待买清单（可复制）
- **宝宝模式开关**：开启后优先推荐宝宝喜爱度高的菜

### 5.2 菜谱管理
- **列表/卡片视图**：展示所有菜谱
- **筛选**：按分类（大荤/小荤/素菜/汤/主食）
- **排序**：按喜爱度 / 最近吃过 / 名称 / 用时
- **搜索**：按菜名或食材模糊搜索
- **编辑**：修改喜爱度、用时、食材（直接操作 localStorage）
- **导入 CSV**：`<input type="file">` 读取用户编辑的 `recipes.csv`，解析后覆盖内置数据
- **导出 CSV**：把当前数据导出为 CSV 下载

### 5.3 历史菜单
- **日历视图**：按月展示，有菜单的日期高亮
- **列表视图**：最近 30 天的菜单记录
- **复用菜单**：一键把某天的菜单复制到今天
- **删除记录**：误操作可删

### 5.4 我的食材
- **添加食材**：名称 + 数量 + 单位
- **列表展示**：显示已录入的库存
- **清空**：一键清空（每天买菜前重置）
- **智能匹配**：生成菜单时优先推荐包含库存食材的菜

## 6. 推荐引擎算法（engine.js）

**核心思路**：每道菜算一个综合得分，组合成菜单后再算组内食材重复惩罚，最终输出**两组**高分菜单供用户选择。

### 6.1 单菜评分
```
score = 喜爱度 + 库存加分 + 时间衰减分

时间衰减（lastEaten 距今天数）：
  1天前（昨天）: -5
  2天前（前天）: -4
  3天前        : -3
  4天前        : -2
  5天前        : -1
  6天前及以上  :  0

库存加分：
  如果该菜的食材清单中，有任意一项与库存食材匹配 => +5
```

### 6.2 菜单组合评分
```
menu_score = 组内各菜单菜分之和 + 食材重复惩罚

食材重复惩罚：
  统计菜单里所有食材的出现次数
  每有 1 个食材出现 2 次 => -3 分
  每有 1 个食材出现 3 次 => -6 分（累加，即 -3 × (count-1)）
  
  示例：番茄炒蛋 + 番茄鸡蛋汤 同时出现，番茄和鸡蛋各重复 1 次 => -6 分
```

### 6.3 生成两组菜单
```
1. 计算所有菜谱的单菜评分，按分类归档
2. 每类取前 N 名（如前 5 名）进入组合池，减少计算量
3. 枚举满足约束的组合：
   - 必须包含 1 道 big_meat
   - 必须包含 1 道 small_meat
   - 必须包含 1 道 vegetable
   - 必须包含 1 道 soup
   - 必须包含 1 道 staple（如不想吃花式主食，可用"白米饭"占位，不占用评分）
4. 对每个合法组合计算 menu_score
5. 按 menu_score 降序排序
6. 取前 2 组作为推荐，但要求两组之间至少替换掉 2 道菜（确保多样性）
```

### 6.4 输出格式
```js
[
  {
    id: "menu_a",
    recipes: ["糖醋排骨", "番茄炒蛋", "手撕包菜", "紫菜鸡蛋汤", "米饭"],
    score: 28,
    scoreBreakdown: { ... }   // 供调试用，显示每道菜得分和惩罚项
  },
  {
    id: "menu_b",
    recipes: ["清蒸鲈鱼", "虾仁蒸蛋", "西兰花炒胡萝卜", "萝卜牛肉汤", "蒸南瓜"],
    score: 25,
    scoreBreakdown: { ... }
  }
]
```

## 7. UI/UX 设计

- **移动优先**：主要场景是用户在手机上点几下生成菜单
- **配色**：暖色调（橙/米白），食欲感
- **导航**：底部 Tab 栏（今日推荐 / 菜谱 / 历史 / 食材）
- **交互**：
  - 按钮够大，方便单手操作
  - 菜单生成后有"截图分享"的排版（方便发给阿姨）
  - 采购清单支持一键复制文字

## 8. 开发阶段

### Phase 1：骨架与数据（1~2h）
- [ ] 搭建 `index.html` + `style.css` 骨架（底部导航、页面切换）
- [ ] `data.js`：CSV 解析器、localStorage 读写封装
- [ ] 把 `recipes.csv` 解析为内嵌默认数据
- [ ] 实现数据导入/导出功能

### Phase 2：核心功能（2~3h）
- [ ] `engine.js`：推荐算法实现
- [ ] 今日推荐页面：生成菜单、换菜、确认、采购清单
- [ ] 我的食材页面：增删改库存

### Phase 3：管理与记录（1~2h）
- [ ] 菜谱管理：列表、筛选、排序、搜索、编辑喜爱度
- [ ] 历史菜单：列表展示、复用、删除

### Phase 4：PWA 与优化（1h）
- [ ] `manifest.json` + 图标
- [ ] `sw.js`：缓存静态资源，实现离线打开
- [ ] 响应式细节优化、动画过渡

## 9. 关键决策

1. **CSV vs JSON**：用户维护 `recipes.csv`，程序启动时解析。App 内编辑直接写 localStorage，导出时再生成 CSV。
2. **备份策略**：支持导出完整数据（菜谱+历史+库存）为 JSON 文件，换手机时导入恢复。
3. **无后端同步**：家庭成员共用同一台手机操作，或截图分享菜单。如需多设备同步，未来可扩展 WebDAV/坚果云备份。
